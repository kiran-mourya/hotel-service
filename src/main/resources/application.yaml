spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hoteldb?createDatabaseIfNotExists=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Kiran@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8082
